# 部署到公网服务器（阿里云 · 47.116.26.211）

> 适配：公网开放端口 **8080–8089**、数据盘 **/data**。
> 门户走 8080，后端与数据库只在内网。国内服务器**走 HF 镜像**避免卡在下载。

---

## 0. 安全第一（务必先做）
- **改掉 SSH 密码或改用密钥登录**（密码已在聊天里明文出现，等于泄露）。
- 编辑 `docker-compose.prod.yml`，把两处 `CHANGE_ME_db_pw` 换成同一个强密码。
- 阿里云安全组只放行 **8080、8081、22**；数据库 5432、后端 8000 不要对公网开。

---

## 1. 装 Docker（服务器上，一次性）
```bash
ssh ecs-user@47.116.26.211
# 安装 docker + compose 插件（Ubuntu）
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker $USER    # 让当前用户免 sudo 用 docker，重登生效
# 退出重连一次
exit
ssh ecs-user@47.116.26.211
docker version && docker compose version   # 验证
```

## 2. 把项目传到服务器
在**本机**（你的电脑）项目目录执行，把整个 `mini_datahub` 传到数据盘：
```bash
# 方式 A：scp（简单）
scp -r ~/Desktop/RoboticDataHub/mini_datahub ecs-user@47.116.26.211:/data/

# 方式 B：git（若已放 GitHub，服务器上 clone 更方便更新）
# ssh 到服务器后： cd /data && git clone <你的仓库地址> mini_datahub
```

## 3. 准备数据盘目录 + 改密码
```bash
ssh ecs-user@47.116.26.211
cd /data/mini_datahub
sudo mkdir -p /data/pgdata /data/hf_cache /data/viz_cache
sudo chown -R $USER:$USER /data/pgdata /data/hf_cache /data/viz_cache
# 改数据库密码（把两处 CHANGE_ME_db_pw 改成同一强密码）
nano docker-compose.prod.yml
```

## 4. 一键启动
```bash
docker compose -f docker-compose.prod.yml up -d --build
docker compose -f docker-compose.prod.yml ps      # 看是否都 Up
```
- 门户： **http://47.116.26.211:8080**
- API 文档： http://47.116.26.211:8081/docs

## 5. 接入数据（首次，容器内跑；走镜像所以不卡）
```bash
# HF/LeRobot 数据集
docker compose -f docker-compose.prod.yml exec backend python 11_batch_ingest.py --limit 200
# OXE（跨源，GCS；国内可能较慢，可先少量）
docker compose -f docker-compose.prod.yml exec backend python 17_ingest_oxe.py --limit 20
# 补规格元数据（文件大小/时长等）
docker compose -f docker-compose.prod.yml exec backend python 16_enrich_metadata.py
```
> 用了 Postgres，**接入时不用停后端**，多人可同时访问。

## 6. 日常运维
```bash
docker compose -f docker-compose.prod.yml logs -f backend   # 看日志
docker compose -f docker-compose.prod.yml restart backend   # 重启
docker compose -f docker-compose.prod.yml down              # 停（数据在 /data 不丢）
git pull && docker compose -f docker-compose.prod.yml up -d --build   # 更新代码后重部署
```

---

## 重要网络提示（国内服务器）
- **接入/模型下载**已配 `HF_ENDPOINT=https://hf-mirror.com`，走镜像。
- **OXE 用的 Google 云存储（GCS）国内可能慢/不通**，接入 OXE 若失败属正常，可换有外网的机器接入后把库文件拷过来。
- **官方 LeRobot 可视化 / 视频回放**是在**用户浏览器**里加载的，速度取决于访问者能否顺畅连 `hf.space` / `huggingface.co`——这块服务器改不了。给国内用户演示时，v3.0 av1 视频建议用 Chrome。

## 可选：HTTPS + 域名
若要 https 和域名，可在前面再加一层 Nginx/Caddy 反代到 8080，用 Let's Encrypt 出证书。研究预览阶段直接用 `http://IP:8080` 也够。
