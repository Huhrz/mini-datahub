# 机器人 DataHub —— 项目进展报告

> 一句话：我做了一个"机器人训练数据的图书馆"——把散落在各处、格式不一的具身智能数据集，聚合进一个能搜索、能筛选、能预览回放的统一门户。
>
> 更新日期：2026-06-30 · 阶段：可运行的 MVP（最小可用版本）

---

## 一、这个项目想解决什么问题

机器人现在靠"看大量示范数据"来学动作（类似 ChatGPT 靠海量文本学说话）。但这些数据有三个痛点：

1. **散**：各家实验室、各大平台各存各的，凑不到一起。
2. **格式乱**：LeRobot、RLDS、HDF5、MCAP… 互不通用，没法一起查。
3. **难挑**：研究已证明"盲目堆数据反而把模型教笨"，但没人帮你判断哪批数据干净、有用、能商用。

**这个 DataHub 就是那个"中央图书馆"**：把别人的数据**联邦聚合**进来（只登记目录卡、不搬运数据本体），统一格式、能搜能筛能预览，并自动给数据打质量分。

---

## 二、到目前为止做了什么

整个项目像搭一条流水线，从"数据进来"到"用户用上"，每一环都做了一个能跑的雏形：

| 环节 | 做了什么 | 对应文件 |
|---|---|---|
| **登记卡设计** | 设计了每个数据集要记哪些信息（本体、许可、动作约定、质量分等 30+ 字段），是整个系统的地基 | `schema.py` |
| **多格式接入** | 一个适配器框架 + 4 种真实格式接入：LeRobot(HF)、RLDS(OXE)、HDF5、MCAP。加新格式只需写一个小函数 | `sources.py` `09_ingest.py` |
| **格式归一** | 把不同结构的数据翻译成同一种标准表示（"粘合层"） | `04_convert_formats.py` |
| **目录检索** | 把元数据存进数据库，支持按本体/许可/格式/质量分等条件筛选 | `hub_data.py` `02_build_catalog.py` |
| **自动质检** | 入库时自动给数据打"质量分"和"可学性分"，揪出脏数据和没信息量的冗余数据 | `quality.py` `05_profile_quality.py` |
| **可视化回放** | 用 Rerun 把一条轨迹播出来看；并实现"跨源统一回放"——两个不同格式的数据集在同一个回放器里看 | `01_…` `08_unified_replay.py` `viz.py` |
| **网页门户** | 一个 Streamlit 网页：搜索、筛选、统计图、数据集详情、一键回放，并做了美化 | `app.py` |
| **健康检查** | 扫描所有数据集链接，标出失效(404)的（联邦目录的链接会失效，需要定期体检） | `07_check_links.py` |

代码已开源在 GitHub。

---

## 三、最关键的一个成果

我成功**联邦接入了一个 Open X-Embodiment 的真实数据集**（`fractal20220817_data`，8.7 万条轨迹）。它托管在 Google 的服务器上、**HuggingFace 根本索引不到**，而我的 hub 能把它和 HF 的数据放进**同一个门户**里一起检索。

**这一条最能说明这个项目的价值定位**：市面上现有的工具——
- **HuggingFace**：能可视化，但只限它自己托管的数据；
- **Festivus**：跨源索引，但不可视化、不能回放；
- **Humaid**：能可视化回放，但只有自家数据。

**"跨源 + 多格式 + 可视化回放"这个交集，目前还没人占住**。这就是这个项目瞄准的"无人区"。

---

## 四、当前能做到的（已验证）

- 同一个目录里同时装着 **lerobot / rlds / hdf5** 三种格式的真实/示例数据；
- 网页里能按本体、许可、格式、采集方式、质量分筛选；
- 点任意数据集能看详情、能弹出 Rerun 回放；
- 自动质检能区分好数据 / 脏数据 / 冗余数据；
- 一条命令接入新数据集，一个小函数支持新格式。

---

## 五、后面的方向

按优先级和价值排：

1. **批量自动接入**：现在是手动一个个接；下一步用 API 一次性枚举并接入某个源下的全部数据集，让目录从几个变成几百上千个。
2. **接入更多真实 OXE/RLDS 数据集**：把"跨源"这个差异点做厚，这是相对 HuggingFace 最硬的护城河。
3. **统一 taxonomy（受控词表）**：把各源五花八门的任务/场景描述映射到一套标准词表，让跨数据集检索真正"准"。这是文档里最难、也最值钱的护城河（B1）。
4. **真实数据的质检与回放**：现在质检/回放主要在示例数据上；接通真实数据的采样质检和真实轨迹回放。
5. **生成 Croissant 元数据**：让数据集能被 Google Dataset Search 自动收录，白送全球曝光。
6. **网页升级**：若要做成对外产品，从 Streamlit 迁到真正的前端，把"可视化+交互+回放"的体验做到惊艳。

---

## 六、一句话总结

> 我用最小规模把一个机器人数据 hub 的**核心链路全部打通了**：多格式接入 → 格式归一 → 目录检索 → 自动质检 → 可视化回放 → 网页门户。其中"跨源 + 多格式 + 可回放"正好是现有工具都没占住的位置。下一步重点是**上规模**（批量接入）和**做厚护城河**（taxonomy + 更多跨源数据）。
