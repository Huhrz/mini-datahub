# 部署说明（依赖隔离 + 一键部署）

本次把依赖做了隔离，**根治了 `sentence-transformers` 与 `lerobot` 的版本冲突**
（就是之前语义向量报 RuntimeError 的原因）。核心思路：把只在"深度质检/真实回放"
才用、且锁死旧版 `huggingface_hub` 的 `lerobot` 从核心环境剥离。

有两条路，按需选。

---

## 路线 A：虚拟环境（最快，立即修好向量冲突）

```bash
cd ~/Desktop/RoboticDataHub/mini_datahub
bash setup.sh          # 建 .venv 并装核心依赖（含可正常工作的 sentence-transformers）
source .venv/bin/activate     # 进入环境，提示符出现 (.venv)
```

之后所有后端/接入命令都在这个环境里跑（干净、无冲突）：

```bash
uvicorn api:app --reload --port 8000        # 后端
python 13_build_embeddings.py               # 现在向量能正常算了
python 11_batch_ingest.py --limit 200       # 接入
deactivate                                  # 退出环境
```

前端另开终端：`cd web && npm install && npm run dev`

> **只有**要用深度质检(`06`)或真实回放(`01 --repo-id`)时，才另建一个环境装 lerobot：
> ```bash
> python3 -m venv .venv-lerobot
> source .venv-lerobot/bin/activate
> pip install -r requirements-optional.txt
> ```
> 两个环境各用各的，互不污染。

---

## 路线 B：Docker 一键部署（对外/上服务器）

需要装 [Docker Desktop](https://www.docker.com/products/docker-desktop/)。

```bash
cd ~/Desktop/RoboticDataHub/mini_datahub
docker compose up --build
```

- 门户： http://localhost:8080
- 后端文档： http://localhost:8000/docs
- 目录数据库持久化在 Docker 卷 `catalog` 里，容器重启不丢。

**接入数据**（在容器里跑）：
```bash
docker compose exec backend python 11_batch_ingest.py --limit 200
```

> ⚠️ **当前限制**：DuckDB 单写锁——摄入写库时后端要先停（`docker compose stop backend`
> → 接入 → `docker compose start backend`）。**下一步换 Postgres 后**后端与摄入就能
> 同时读写，不用停。

停止：`docker compose down`（加 `-v` 连数据卷一起删）。

---

## 路线 C：Postgres 生产部署（后端与摄入可并行，无单写锁）

DuckDB 单写锁的痛点（摄入要停后端）由 **Postgres** 彻底解决——后端和摄入
**同时读写**互不干扰。代码已做数据库抽象（`store.py`），**同一套代码**靠环境变量
`MDH_DB` 切换后端：

- `MDH_DB` 不设 → DuckDB（本地默认）
- `MDH_DB=postgresql://user:pass@host:5432/db` → Postgres（生产）

**一键起（含 Postgres 容器）：**
```bash
docker compose -f docker-compose.yml -f docker-compose.postgres.yml up --build
```
- 门户 http://localhost:8080，后端自动连 Postgres。
- **接入数据时不用停后端了**：
  ```bash
  docker compose -f docker-compose.yml -f docker-compose.postgres.yml \
      exec backend python 11_batch_ingest.py --limit 200
  ```
  （后端仍在服务，摄入同时写库，互不锁死。）

> 说明：核心服务（后端 API、`hub_data`、`embeddings`、`11_batch_ingest`）已适配
> Postgres；`02/03/09/12` 等少数命令行脚本目前仍按 DuckDB 写法，若在 Postgres 下用到
> 再逐个适配即可（本地 DuckDB 下它们照常工作）。Postgres 路径未在本机沙箱端到端跑过
> （沙箱装不了 PG 服务），首次用 Docker 起来时若有小问题，把报错发我快速修。

---

## 文件说明

| 文件 | 作用 |
|---|---|
| `requirements.txt` | 核心依赖（**不含 lerobot**，避免冲突） |
| `requirements-optional.txt` | lerobot/rerun（单独环境，可选） |
| `setup.sh` | 一键建虚拟环境 + 装核心依赖 |
| `Dockerfile` | 后端镜像（FastAPI + 摄入 + 语义搜索） |
| `web/Dockerfile` `web/nginx.conf` | 前端镜像（构建 React → Nginx 托管 + 反代 /api） |
| `docker-compose.yml` | 一键启动后端 + 前端 |
| `docker-compose.postgres.yml` | 生产变体：Postgres + 定时调度器 |
| `store.py` | 数据库抽象层（DuckDB / Postgres 二选一） |
| `pipeline.py` | 一次刷新流水线（增量接入 + 补向量/概念 + 健康检查） |
| `scheduler.py` | 定时调度器（每 N 小时自动跑 pipeline） |

数据库路径可用环境变量 `MDH_DB` 覆盖（Docker 已设为挂卷 `/data/catalog.duckdb`）。

---

## 路线 D：自动化摄入调度（让数据保持新鲜）

数据 hub 的生命线是"数据不过期"。`scheduler.py` 每隔 N 小时自动：
**增量接入新数据集 → 补语义向量 + 概念标签 → 链接健康检查（写回 link_health 表）**。

**手动跑一次刷新**（本地，venv 里）：
```bash
python pipeline.py --limit 50 --task robotics
```

**生产：随 Postgres 变体常驻定时跑**（已含在 `docker-compose.postgres.yml`）：
```bash
docker compose -f docker-compose.yml -f docker-compose.postgres.yml up --build
```
调度器作为独立服务运行，配置（在 compose 里改）：
- `MDH_SCHEDULE_HOURS=24`  刷新间隔
- `MDH_INGEST_TASK=robotics` / `MDH_INGEST_AUTHORS=lerobot,agibot-world`  接入范围
- `MDH_INGEST_LIMIT=50`  每轮目标数

> 调度器会写库，所以**必须配 Postgres**（和后端并发读写）；DuckDB 下会抢写锁。
> 链接检查结果写进 `link_health` 表（失效链接对应文档"联邦指针失效"风险），门户可据此标记失效数据集。
