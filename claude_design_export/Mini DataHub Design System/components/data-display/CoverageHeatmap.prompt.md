Embodiment × task-concept coverage heatmap, single-hue accent ramp with gray gaps.

```jsx
<CoverageHeatmap
  embodiments={["single_arm", "bimanual"]}
  concepts={[{id:"pick_place", label:"抓取放置"}]}
  cells={[{embodiment:"single_arm", concept:"pick_place", count: 42}]}
/>
```
