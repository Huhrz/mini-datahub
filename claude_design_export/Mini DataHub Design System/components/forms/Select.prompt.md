Labeled select dropdown, used for each facet filter.

```jsx
<Select label="本体类型" value={embodiment} onChange={setEmbodiment}
  options={[{value:"", label:"（不限）"}, {value:"bimanual", label:"bimanual"}]} />
```
